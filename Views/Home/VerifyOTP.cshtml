

@{
    Layout = null;
}

<link href="~/themeassets/demo.dashboardpack.com/architectui-html-pro/main.d810cf0ae7f39f28f336.css" rel="stylesheet" />
<title>Verify OTP</title>
<div class="app-container app-theme-white body-tabs-shadow">
    <div class="app-container">
        <div class="h-100 bg-plum-plate bg-animation">
            <div class="d-flex h-100 justify-content-center align-items-center">
                <div class="mx-auto app-login-box col-md-6">
                    <div class="modal-dialog w-100">
                        <div class="modal-content">
                            <div class="modal-header">
                                <div class="h5 modal-title text-center">
                                    Enter OTP
                                    <h6 class="mt-1 mb-0 opacity-8">
                                        <span>Check your email and enter the verification code. Please wait a few seconds for the OTP to arrive.</span>
                                    </h6>
                                </div>
                            </div>
                            <div class="modal-body">
                                @using (Html.BeginForm("VerifyOTP", "Home", FormMethod.Post))
                                {
                                    @Html.AntiForgeryToken()

                                    if (ViewBag.Error != null)
                                    {
                                        <div class="alert alert-danger">@ViewBag.Error</div>
                                    }

                                    <div class="form-row">
                                        <div class="col-md-12">
                                            <div class="position-relative form-group">
                                                <label for="otp">OTP</label>
                                                <input type="text" name="otp" class="form-control" placeholder="Enter OTP code" required />
                                            </div>
                                        </div>
                                    </div>

                                    <div class="modal-footer clearfix">
                                        <div class="float-left">
                                            <a href="@Url.Action("Login", "Home")" class="btn btn-outline-secondary btn-lg">Back to Login</a>
                                        </div>
                                        <div class="float-right">
                                            <button type="submit" class="btn btn-success btn-lg">Verify & Update</button>
                                        </div>
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                    <div class="text-center text-white opacity-8 mt-3">
                        Copyright © Acme Digitek Solutions Pvt. Limited
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

